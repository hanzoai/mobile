<?xml version="1.0" encoding="utf-8"?>
<!-- RemoteViews layout: framework views only (LinearLayout, TextView), no
     app widgets. Ids are resolved by name from WidgetProvider, which lives
     in a library and cannot see this R class — rename an id and the provider
     must follow. Default texts keep the picker preview and the first frame
     honest before the provider's first pass. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/card"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:orientation="vertical"
  android:background="@drawable/widget_bg"
  android:padding="14dp">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <TextView
      android:id="@+id/mark"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:text="Hanzo"
      android:textColor="#fafafa"
      android:textSize="13sp"
      android:textStyle="bold"
      android:letterSpacing="0.02"
      android:maxLines="1" />

    <TextView
      android:id="@+id/updated"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:textColor="#66fafafa"
      android:textSize="11sp"
      android:maxLines="1" />
  </LinearLayout>

  <TextView
    android:id="@+id/snippet"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:layout_marginTop="8dp"
    android:text="No activity yet"
    android:textColor="#fafafa"
    android:textSize="14sp"
    android:lineSpacingExtra="3dp"
    android:maxLines="4"
    android:ellipsize="end"
    android:gravity="top" />

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_marginTop="10dp">

    <TextView
      android:id="@+id/voice"
      android:layout_width="0dp"
      android:layout_height="32dp"
      android:layout_weight="1"
      android:layout_marginEnd="6dp"
      android:background="@drawable/widget_chip"
      android:gravity="center"
      android:text="Voice"
      android:textColor="#ccfafafa"
      android:textSize="11sp"
      android:maxLines="1" />

    <TextView
      android:id="@+id/file"
      android:layout_width="0dp"
      android:layout_height="32dp"
      android:layout_weight="1"
      android:layout_marginEnd="6dp"
      android:background="@drawable/widget_chip"
      android:gravity="center"
      android:text="File"
      android:textColor="#ccfafafa"
      android:textSize="11sp"
      android:maxLines="1" />

    <TextView
      android:id="@+id/image"
      android:layout_width="0dp"
      android:layout_height="32dp"
      android:layout_weight="1"
      android:layout_marginEnd="6dp"
      android:background="@drawable/widget_chip"
      android:gravity="center"
      android:text="Image"
      android:textColor="#ccfafafa"
      android:textSize="11sp"
      android:maxLines="1" />

    <TextView
      android:id="@+id/incognito"
      android:layout_width="0dp"
      android:layout_height="32dp"
      android:layout_weight="1"
      android:background="@drawable/widget_chip"
      android:gravity="center"
      android:text="Private"
      android:textColor="#ccfafafa"
      android:textSize="11sp"
      android:maxLines="1" />
  </LinearLayout>
</LinearLayout>
