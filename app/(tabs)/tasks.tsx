import { Empty } from '@/components/empty'

export default function Tasks() {
  return (
    <Empty
      title="Tasks"
      detail="Long-running work lands here. No runs exist yet and nothing is bound to the run registry."
    />
  )
}
