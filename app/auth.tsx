import { Empty } from '@/components/empty'

export default function Auth() {
  return (
    <Empty
      title="Auth"
      detail="OIDC redirect target hanzo://auth. The auth agent replaces this screen."
    />
  )
}
